<%= provide(:title,  t('titles.school_classes.index')) %>
<%= provide :breadcrumb do %>
    <li class="active"><%= t('shared.nav.school_classes') %></li>
<% end %>
<%= provide :sidebar do %>
    <%=  form_for @search do |f| %>
        <li class="nav-header"><%= f.label :name_contains %></li>
        <li><%= f.text_field :name_contains %></li>
        <hr/>
        <li class="nav-header"><%=t 'helpers.order'  %></li>
        <%= f.sort_link :id %> |
        <%= f.sort_link :name %>
        <hr/>
        <%= f.submit t('helpers.links.search'), class: 'btn btn-primary' %> <%= link_to t('helpers.actions.clear'), school_classes_path, class: 'btn' %>
    <% end %>
<% end %>
<div class="page-header">
  <h2><%=  t('titles.school_classes.index') %></h2>
</div>
<table class="table table-striped">
  <thead>
  <tr>
    <th>Id</th>
    <th><%= t_field('school_class','name') %></th>
    <th><%= t('helpers.created_at') %></th>
    <th><%=t 'helpers.actions.all' %></th>
  </tr>
  </thead>
  <tbody>
  <%= render @school_classes %>
  </tbody>
</table>
<%= paginate @school_classes %>
<%= link_to t("helpers.actions.new_f"),
            new_school_class_path,
            :class => 'btn btn-primary' %>
